"Nginx and LetsEncrypt on autopilot" examples without SSL
=========================================================

To run this example:

```bash
docker-compose up -d
curl 'http://localhost/blog/'
curl 'http://localhost/about/'
```
